import { PhasemilestonesPipe } from './phasemilestones.pipe';

describe('PhasemilestonesPipe', () => {
  it('create an instance', () => {
    const pipe = new PhasemilestonesPipe();
    expect(pipe).toBeTruthy();
  });
});
