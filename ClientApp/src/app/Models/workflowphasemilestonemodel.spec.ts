import { Workflowphasemilestonemodel } from './workflowphasemilestonemodel';

describe('Workflowphasemilestonemodel', () => {
  it('should create an instance', () => {
    expect(new Workflowphasemilestonemodel()).toBeTruthy();
  });
});
