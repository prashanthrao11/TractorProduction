import { ProductionUserApproval } from './production-user-approval';

describe('ProductionUserApproval', () => {
  it('should create an instance', () => {
    expect(new ProductionUserApproval()).toBeTruthy();
  });
});
