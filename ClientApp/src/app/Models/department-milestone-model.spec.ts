import { DepartmentMilestoneModel } from './department-milestone-model';

describe('DepartmentMilestoneModel', () => {
  it('should create an instance', () => {
    expect(new DepartmentMilestoneModel()).toBeTruthy();
  });
});
