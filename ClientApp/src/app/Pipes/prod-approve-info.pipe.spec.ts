import { ProdApproveInfoPipe } from './prod-approve-info.pipe';

describe('ProdApproveInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProdApproveInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
