import { ApproverItemPipe } from './approver-item.pipe';

describe('ApproverItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ApproverItemPipe();
    expect(pipe).toBeTruthy();
  });
});
